"""Converts a normlized timeseries to SAX symbols."""
import numpy as np


def series2sax(series, alphabet_cuts):
    """Simple SAX implementation."""
    series_len = len(series)
    sax = list()

    return sax
